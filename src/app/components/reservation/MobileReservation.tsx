import { ReservationProps } from '@/types/reservation';

export default function MobileReservation({ price, activityId }: ReservationProps) {
  return (
    <div>
      <div>MobileReservation</div>
    </div>
  );
}
