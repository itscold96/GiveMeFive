import Button from './components/Button/Button';

export default function Test() {
  return (
    <div>
      <span>test</span>
      <Button buttonWidth="full" buttonColor="gray" borderRadius="radius6" textSize="md" padding="padding8">
        로그인 하기
      </Button>
    </div>
  );
}
